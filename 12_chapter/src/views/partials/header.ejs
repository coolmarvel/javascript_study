<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous" />
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>

  <div class="container">
    <% if (error && error.length> 0) { %>
    <div class="alert alert-dange" role="alert">
      <%= error %>
    </div>
    <% } %>
    <% if (success && success.length> 0) { %>
    <div class="alert alert-success" role="alert">
      <%= success %>
    </div>
    <% } %>
  </div>

  <div class="navbar navbar-dark shadow-sm fixed-top navbar-expand-lg">
    <div class="container">
      <a href="/" class="navbar-brand">Shop</a>
      <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarToggle">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarToggle">
        <% if (currentUser&&currentUser.admin===1) { %>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a href="/admin/products" class="nav-link">Products</a>
          </li>
          <li class="nav-item">
            <a href="/admin/categories" class="nav-link">Categories</a>
          </li>
        </ul>
        <% } %>

        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <% if (currentUser) { %>
          <li class="nav-item">
            <form action="/uath/logout" method="post">
              <button type="submit" class="nav-link btn btn-link" style="margin-top: 1px;">
                Logout
              </button>
            </form>
          </li>
          <% } else { %>
          <li class="nav-item">
            <a href="/signup" class="nav-link">Signup</a>
          </li>
          <li class="nav-item">
            <a href="/login" class="nav-link">Login</a>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </div>